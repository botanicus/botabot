=== Core ===
- status
- per profiles data stores (SQLite)
- history
- handle direct messages
- console
- roles
- callbacks
- plugin/callback management (bin/plugin.rb)
- rewrite plugins, it should be POR (plain old ruby) class < BotaBot::Plugin and instance method run defined ... any funky DSL anymore!
- code reloading seems to be a bit complicated (need to restart some parts of bot?)

    @client = Jabber::Client.new(Jabber::JID.new(jid))
    @client.connect
    @client.auth(password)
    presence = Jabber::Presence.new
    presence.set_show(:chat).set_status("I'm just sending some SMS ...")
    @client.send(presence)

=== Bugs ===
Kdyz je ve dvou roomech, na PM reaguje, ale na room message jen v tom do kteryho se pripojil naposled

=== Project ===
- rakefile, specfile

=== Plugins ===
- vzkazovnicek
- learning new gags
- get things work
